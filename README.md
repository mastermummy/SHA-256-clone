Hello! This python script implements SHA-256. Simply enter an input string, and the corresponding hash digest is provided. 

The function that does the calculation is 'sha256', which accepts an input string.
